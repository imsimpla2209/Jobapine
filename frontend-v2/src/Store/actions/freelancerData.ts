export const freelancerDataAction = () => (dispatch) => {
    try {
        let res;
        // db.collection("freelancer")
        //     .doc(auth?.currentUser?.uid)
        //     .onSnapshot(doc => {
        //         res = doc.data();
        //         dispatch({
        //             type: "USER_DATA",
        //             payload: res,
        //         });
        //     });

    } catch (err) {
        console.log(err);
    }
};
